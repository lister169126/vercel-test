name: Test Workflow
on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for open pull requests
        id: check-pr
        run: |
          response=$(curl -s -H "Accept: application/vnd.github.v3+json" \
                          -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                          "https://api.github.com/repos/${{ github.repository }}/pulls?state=open&head=${{ github.repository_owner }}:${{ github.ref_name }}")
          echo "response='$response'" >> $GITHUB_ENV

      - name: show response
        run: echo ${{ steps.check-pr.outcome }}

      - name: Run tests
        if: steps.check-pr.outcome
        run: echo "Existuje PR"

      - name: Run tests
        if: steps.check-pr.outcome == 0
        run: echo "Neexistuje PR"
